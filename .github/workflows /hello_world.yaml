name: Hello World
on:
  push:
    branches:
      - main
jobs:
  fist-hello-world-job:
    run-on: ubuntu-latest
    steps:
      - name: checkout code
      uses: actions/checkout@v3
      - name: Print message
      run: echo "Hello World GA testing"
      
